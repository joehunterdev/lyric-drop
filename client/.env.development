# Development Environment
VITE_DEV_HOST=lyric-drop.joehunter.local
VITE_DEV_PORT=8081
VITE_DEBUG=true

# API Configuration (Phase 3 - not used yet)
# Same origin as frontend - no CORS issues
VITE_API_URL=/api

# ===========================================
# Export Settings
# ===========================================

# Video bitrate in bits per second (higher = better quality, larger file)
# 5000000 = 5 Mbps (good), 10000000 = 10 Mbps (better), 15000000 = 15 Mbps (high)
VITE_EXPORT_BITRATE=15000000

# Export frame rate (fps)
VITE_EXPORT_FRAMERATE=30

# Export format (webm is the only browser-native option)
VITE_EXPORT_FORMAT=webm

# ===========================================
# File Upload Settings  
# ===========================================

# Allowed video file extensions (comma-separated)
VITE_ALLOWED_VIDEO_EXTENSIONS=.mp4,.webm

# Allowed video MIME types (comma-separated)
VITE_ALLOWED_VIDEO_TYPES=video/mp4,video/webm

# Maximum file size in bytes (500MB = 524288000, 1GB = 1073741824)
VITE_MAX_FILE_SIZE=1073741824

# ===========================================
# Lyric Settings
# ===========================================

# Maximum number of lyric segments
VITE_MAX_SEGMENTS=500

# Default font size as percentage of video height
VITE_LYRIC_FONT_SIZE_PERCENT=5

# Default background opacity (0-1)
VITE_LYRIC_BG_OPACITY=0.7
